name: 'tg-issue-47'

on:
  workflow_dispatch:

env:
  tf_version: '1.4.7'
  tg_version: '0.46.3'
  working_dir: 'tg-action-issue-43/example/module1'
  environment: 'example'

jobs:

  run:
    runs-on: ubuntu-latest
    steps:

      - name: 'Checkout'
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          submodules: true

      - name: Plan
        uses: gruntwork-io/terragrunt-action@v2
        with:
          tf_version: ${{ env.tf_version }}
          tg_version: ${{ env.tg_version }}
          tg_dir: "${{ env.working_dir }}/${{ env.environment }}"
          tg_command: 'run-all plan -out tfplan'

      - name: Apply
        uses: gruntwork-io/terragrunt-action@v2
        with:
          tf_version: ${{ env.tf_version }}
          tg_version: ${{ env.tg_version }}
          tg_dir: "${{ env.working_dir }}/${{ env.environment }}"
          tg_command: >-
            apply
            --var-file devl.tfvars
