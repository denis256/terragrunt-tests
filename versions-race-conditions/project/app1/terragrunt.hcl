include {
  path   = find_in_parent_folders()
}

dependency "dep_1" {
  config_path = "../dep/dep-1"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_2" {
  config_path = "../dep/dep-2"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_3" {
  config_path = "../dep/dep-3"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_4" {
  config_path = "../dep/dep-4"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_5" {
  config_path = "../dep/dep-5"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_6" {
  config_path = "../dep/dep-6"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_7" {
  config_path = "../dep/dep-7"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_8" {
  config_path = "../dep/dep-8"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_9" {
  config_path = "../dep/dep-9"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_10" {
  config_path = "../dep/dep-10"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_11" {
  config_path = "../dep/dep-11"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_12" {
  config_path = "../dep/dep-12"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_13" {
  config_path = "../dep/dep-13"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_14" {
  config_path = "../dep/dep-14"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_15" {
  config_path = "../dep/dep-15"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_16" {
  config_path = "../dep/dep-16"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_17" {
  config_path = "../dep/dep-17"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_18" {
  config_path = "../dep/dep-18"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_19" {
  config_path = "../dep/dep-19"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_20" {
  config_path = "../dep/dep-20"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_21" {
  config_path = "../dep/dep-21"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_22" {
  config_path = "../dep/dep-22"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_23" {
  config_path = "../dep/dep-23"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_24" {
  config_path = "../dep/dep-24"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}
dependency "dep_25" {
  config_path = "../dep/dep-25"
  mock_outputs_allowed_terraform_commands = ["validate"]
  mock_outputs = {
    vpc_id = "fake-vpc-id"
  }
}



