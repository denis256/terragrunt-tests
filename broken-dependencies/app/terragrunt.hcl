dependency "dependency" {
  config_path = "../dependency"

  mock_outputs = {
    test = "value"
  }

}

dependency "dependency2" {
  config_path = "../dependency2"

  mock_outputs = {
    test = "value"
  }

}
